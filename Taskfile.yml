version: "3"

silent: true

output: prefixed

tasks:
  release:
    - |
      git c
      git tag -d v0.0 || true
      git push origin --delete v0.0 || true
      git tag v0.0
      git push origin v0.0
