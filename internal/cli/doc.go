// Package cli implements the command structure and execution for the tool.
// It defines the root command, subcommands, and handles loading embedded
// configuration files required for the application.
package cli
