// Package progress implements file transfer progress tracking with concurrent
// progress bars (Tracker) and a no-op implementation (Noop) for when tracking
// is disabled, both implementing the Trackable interface.
package progress
