- idelchi/gocry
- idelchi/gogen
- idelchi/go-match
- idelchi/gonc
- idelchi/go-next-tag
- idelchi/tcisd
- name: idelchi/wslint
  version:
    commands: -v

- name: gitlab-org/cli
  description: glab is an open source GitLab CLI tool
  exe: glab
  aliases:
    - gl
  hints:
    - pattern: "*.zip"
      match: '{{ if eq .OS "windows" }}required{{ else }}weighted{{ end }}'
    - pattern: "*.tar.gz"
      match: '{{ if eq .OS "windows" }}weighted{{ else }}required{{ end }}'
  tags:
    - terminal
  inherit: gitlab

- name: mikefarah/yq
  description: YAML, JSON, and XML processor
  exe:
    patterns: yq_*
  hints:
    - pattern: "*.zip"
      match: '{{ if eq .OS "windows" }}required{{ else }}weighted{{ end }}'
  tags:
    - processing

- name: cli/cli
  description: GitHub's official command line tool
  exe:
    name: gh
    patterns:
      - "bin/gh{{ .EXTENSION }}"
  aliases:
    - shawtay
  tags:
    - terminal
