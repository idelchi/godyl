- name: cli/cli
  exe:
    name: gh
    patterns:
      - bin/gh{{ .Platform.Extension }}$
  post:
    - "{{ .Output }}/{{ .Exe.Name }} extension install github/gh-copilot"
    - "{{ .Output }}/{{ .Exe.Name }} extension upgrade gh-copilot"
    - echo 'eval "$(gh copilot alias -- bash)"' >> ~/.bashrc
