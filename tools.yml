- name: TomWright/dasel
  # fallbacks:
  #   - go
  extensions:
    - ""
  source:
    type: |-
      {{- if has .Platform.OS.Name (list "linux" "darwin") -}}
          github
      {{- else -}}
          go
      {{- end -}}
