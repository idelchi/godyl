- name: ofabry/go-callvis
  description: A development tool to help visualize call graph of a Go program using interactive view.
  source:
    type: go
  tags:
    - go
# cd ws
# DEBUG=true godyl --env-file=tokens.env -v install --output=~/.local/bin tools2.yml --tags=ofabry/go-callvis
